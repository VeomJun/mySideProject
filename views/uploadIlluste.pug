extends layout/main

block here
    .upload
        a(href=`/upload${routes.uploadComics}`)
            button.upload-btn__comics Comics
        a(href=`/upload${routes.uploadSketch}`)
            button.upload-btn__sketch Sketch
        a(href=`/upload${routes.uploadNotice}`)
            button.upload-btn__notice  Notice
        a(href=`/upload${routes.uploadBackground}`)
            button.upload-btn__background  Background
    .form-container
        form(action=`/upload${routes.uploadIlluste}`, method="post", enctype="multipart/form-data")
            label(for="file") Thumbnail File
            input(type="file", id="file", name="illusteFile", required=true, multiple)
            label(for="file") Illustes File
            input(type="file", id="file", name="noIllusteFile", required=true, multiple)
            input(type="text", placeholder="Title", name="title", required=true)
            textarea(name="description", placeholder="Description", required=true)
            input(type="submit", value="Upload Illuste")