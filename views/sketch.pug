extends layout/main
include mixins/sketchBlock

block here
    .square
        each content in sketch
            +sketchBlock({
                id:content.id,
                fileUrl:content.fileUrl,
                title:content.title,
                description:content.description
            })