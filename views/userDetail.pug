extends layout/main

block here
    .user-detail
        img(src=user.avatarUrl)
        .user-container
            .user-title
                h1=user.name
                if user.id === loggedUser.id && loggedUser.id === myId
                    a.form-container__link(href=`/users${routes.editProfile}`)
                        button 프로필 수정
                    a.form-container__link(href=`/users${routes.changePassword}`)
                        button 비밀번호 수정
                    a.form-container__link(href=`/upload${routes.uploadDetail}`) 
                        button Upload
                else if user.id === loggedUser.id
                    a.form-container__link(href=`/users${routes.editProfile}`)
                        button 프로필 수정
                    a.form-container__link(href=`/users${routes.changePassword}`)
                        button 비밀번호 수정
            .user-description
                h1=user.description